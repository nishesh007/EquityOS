/**
 * Thin named services for Sprint 10D.5 — shared logic lives in
 * eventLinkingService + portfolioEventService to avoid duplication.
 */

export {
  watchlistEventService,
  buildWatchlistEventInsights,
} from "@/src/core/events/integration/portfolioEventService";
